worker_processes 1;

events {
  worker_connections 1024;
}

http {
  server {
    listen 80 default_server;
    server_name _;

    location / {
      if ($http_authorization != "secret") {
        return 403;
      }

      return 200 "hello from backend";
    }
  }
}
